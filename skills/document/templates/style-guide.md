# <Component or pattern name>

<!-- Copy this file to docs/style-guide/[slug]/index.md — do not edit in place. -->

Date: YYYY-MM-DD

## Status

<Choose one: Draft | Accepted | Superseded>

<!-- If this entry supersedes an existing one, add on the second line under Status:
Supersedes [slug](slug.md) -->

## Overview

<What this component or pattern is and why it exists. One short paragraph that gives a reader enough context to know whether this entry applies to their work.>

## Design tokens

<The visual constants this component relies on: colors, spacing, typography, border-radius, shadow, etc. List each token with its resolved value or CSS custom property name.>

## States

<Every interactive or conditional state the component can be in (default, hover, focus, active, disabled, error, loading, empty, etc.). Describe the visual treatment for each state.>

## Usage rules

<When to use this component or pattern, when NOT to use it, and any constraints the implementation must satisfy. Include dos and don'ts that are not obvious from the other sections.>

<!-- Optional: include only when a concrete HTML example applies -->
## HTML example

<Minimal, self-contained HTML snippet that demonstrates correct markup. Omit this section if no canonical markup structure exists.>
